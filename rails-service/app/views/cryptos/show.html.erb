<p id="notice"><%= notice %></p>

<h1>Cryptos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cryptos.each do |crypto| %>
      <tr>
      <%= crypto.name %><%= crypto.price %><br />
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Nouvelle recherche", root_path, class: "btn btn-secondary" %>